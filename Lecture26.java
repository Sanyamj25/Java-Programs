public class Lecture26 {
    public static void main(String[] args) {
        //Java Collection Framework
        //list queue set map
        /*List interface
         arrylist,linkedlist,vector(stack)
         Queue interface
         priorityQueue,LinkedList,Deque(arrayDeque)
         Set Interface
         HashSet,LinkedHashSet,SortedSet(TreeSet)
         Map interface
         Hashmap,LinkedHashmap,Sortedmap(treemap),Hashtable
         */
    }
}
